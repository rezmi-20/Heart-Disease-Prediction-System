# Heart Disease Prediction — Streamlit App

Simple Streamlit app that predicts heart disease risk using a pre-trained model.

How to run locally:

```bash
python -m pip install -r requirements.txt
python -m streamlit run web_app.py
```

Notes:

- `model.joblib` is excluded from the repository by `.gitignore`. Add the model to the repo if you want the app deployed to Streamlit Community Cloud, or load it from a cloud storage path.
